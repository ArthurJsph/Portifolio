{
    "builds": [
      { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "portifolio/dist" } }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/index.html" }
    ]
  }